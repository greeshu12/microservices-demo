@Library('jenkins-shared-library') _
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                myBuild()
            }
        }

        stage('Deploy') {
            steps {
                myDeploy("service2")
            }
        }
    }

    post {
        always {
            myNotify("Service2 pipeline completed")
        }
    }
}
